spring:
  application:
    name: demo_api
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: config_server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/