spring:
  application:
    name: apigateway
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: configserver
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
