spring:
  application:
    name: apigateway
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: configserver
eureka:
  client:
    service-url:
      defaultZone: http://172.20.25.142:8000/eureka/
