spring:
  application:
    name: config_server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:D:/Work/study/springclouddemo-dev/ConfigServer/yamls/
server:
  port: 8001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/