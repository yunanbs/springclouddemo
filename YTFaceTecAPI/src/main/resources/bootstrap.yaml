spring:
  application:
    name: faceapi
  cloud:
    config:
      profile: demo
      discovery:
        enabled: true
        service-id: configserver
eureka:
  client:
    service-url:
      defaultZone: http://172.20.22.11:8181/eureka/