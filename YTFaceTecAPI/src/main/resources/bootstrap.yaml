spring:
  application:
    name: faceapi
  cloud:
    config:
      profile: demo
      discovery:
        enabled: true
        service-id: configserver
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8181/eureka/